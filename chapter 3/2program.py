# Tuples in python
# a built-in-data type lets us create immutable sequence of values.

tup = (2,1,3,1)
print(type(tup))
print(tup[0])
# tup[0] = 5 tuple object does not support item assignment

# tupel method
tup.index() # return index of first occurrence
tup.count() # counts total occurrences 
